variables:
  DEPLOY_PATH: /var/www/virtual/website

deploy_test:
  stage: deploy
  variables:
    GIT_STRATEGY: fetch
  environment:
    name: test
    url: https://devcms.psenterprise.com
  only:
    - develop
  script:
    - rsync -avzh --no-owner --exclude '.git' $CI_PROJECT_DIR'/' $DEPLOY_PATH --delete