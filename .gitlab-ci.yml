variables:
  DEPLOY_PATH: /var/www/virtual/website

deploy_test:
  stage: deploy
  environment:
    name: staging_test
    url: https://devcms.psenterprise.com
  only:
    - develop
  script:
    - rsync -avzh --exclude '.git' $CI_PROJECT_DIR'/' $DEPLOY_PATH --delete
